# 2026-09-09 阶段发布与实际验收

本轮已将当前工作区服务端、节点和前端发布到实际环境，代码尚未提交或推送。

## 发布结果

| 产物 | SHA-256 |
| --- | --- |
| Server | `6c45f2149b1c101707f9bc1139199a676fad2007ffb235c117975ac1556ea56f` |
| Node | `3ddbf23dca359747a2f0eac640a0b4ffc13211861ed54b4c94d6b9b70e065348` |
| 最终前端 | `1e625ec00c0260eaf19a99a192ba506e3aeab82de2d74c86604b698ea11bddd3` |

- Server `103.236.69.112`：服务 active，本机及公网健康检查通过，公网首页哈希与最终构建一致。
- 北京 `140.143.162.240`、成都 `47.109.18.81`：发布节点下载产物后，两台节点于 09:56 自动升级并重新执行；运行进程 `/proc/<pid>/exe` 的哈希均与上述 Node 一致。
- Node 版本字符串仍为 `0.3.39`，本次以二进制摘要区别构建，不能仅看版本字符串判断是否更新。
- 21 条数据库迁移校验值全部一致，无待执行迁移，没有执行数据库结构变更。

## 验证证据

- 前端 lint、类型检查、147 项测试（35 文件、615 断言）及生产构建通过。
- Linux Release 全量构建通过；CTest 实际通过 27 项，10 项 PostgreSQL 依赖测试跳过。此前 Windows 的 10 项数据库回归实际通过，不能替代 Linux 数据库测试。
- 通过 `curl --resolve` 分别连接两台实际节点，以 `test.a-z.xin` 进行正常证书校验的 HTTPS 请求，均返回 200；响应体与当时已发布的前端构建完全一致。
- 两次请求使用独立的 `flexedge_qa=20260909-final-beijing/chengdu` 标记。数据库分别查到对应节点的唯一记录，核对 Host、GET、HTTPS、200、响应字节数及查询参数。
- 升级后节点继续上报心跳，应用规格分别为北京 2、成都 9，原活动发布保持，未记录应用错误。
- 真实浏览器检查了桌面与窄屏、明暗主题、侧栏折叠、全局搜索、实际网站和节点表格、筛选空状态、用户菜单与退出确认对话框。
- 实际主动退出后跳转登录页，没有“未登录”通知。修复仅静默会话探测的预期 401；测试保留其他查询错误与登录 mutation 错误通知。
- 顶部用户按钮、侧栏底部和用户菜单统一复用无底色的人像图标。登录页删除固定站点数量、在线率和虚构流量图，改为功能说明。

## 备份与限制

- 完整 Server 原产物和数据库备份：`/opt/flexedge/backups/final-server-20260909-095556`。
- 最后一次前端替换前的页面：`/opt/flexedge/backups/final-avatar-20260909-100604/index.html`。
- 完整发布前未单独取得两台节点的状态快照；节点通过已有自动升级机制更新，没有执行准备好的手工节点发布脚本。后续发布 Node 下载产物前，应先完成节点状态备份，不能假设节点会等待手工升级。
- 未实际执行网络断线、日志补交和 ACK 丢失故障测试；未做性能压测，也未覆盖全部业务写入及所有资源页面的错误状态。
- 本次证明实际节点恢复既有配置、TLS 转发及日志交付，不代表新证书签发或任意故障下的数据持久性保证。
